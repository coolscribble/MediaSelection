services:
  mediapicker:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - C:/Users/lilci/Desktop/docker/mediapicker_data:/data
    restart: unless-stopped
    environment:
      - PORT=3000
      - DATA_DIR=/data
      - NODE_ENV=production
      - ALLOWED_ORIGIN=https://mediapicker.cipraland.cc
      - LOCAL_PASSCODE=7$7n%g#%%tR7AUeDAvlr
