services:
  mediapicker:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - C:/Users/lilci/OneDrive/Desktop/docker/mediapicker_data:/data
    restart: unless-stopped
    environment:
      - PORT=3000
      - DATA_DIR=/data
